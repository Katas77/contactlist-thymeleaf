server:
 port: 8080
spring:
 datasource:
  url: jdbc:postgresql://localhost:5432/contacts
  username: user
  password: password
  hikari:
   schema: contacts_schema


# URL локального приложения:
# http://localhost:8080/